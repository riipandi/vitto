{{ layout "layouts/site.vto" }}

{{ setMetadata('title', 'Blog') }}

<!-- Hero -->
<section class="relative overflow-hidden">
  <div class="absolute inset-0 -z-10">
    <div class="absolute inset-0 bg-gradient-to-b from-gray-50 to-gray-100/50 dark:from-gray-900 dark:to-gray-900"></div>
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[600px] h-[400px] bg-gradient-to-b from-blue-200/15 via-transparent to-transparent dark:from-blue-500/8 dark:via-transparent dark:to-transparent rounded-full blur-3xl"></div>
  </div>

  <div class="max-w-3xl mx-auto px-5 sm:px-8 pt-20 pb-12 md:pt-28 md:pb-16 text-center">
    <h1 class="slide-up text-4xl md:text-5xl font-bold tracking-tight text-gray-900 dark:text-white mb-4">
      Blog
    </h1>
    <p class="slide-up-delay-1 text-lg text-gray-500 dark:text-gray-400">
      Updates, ideas, and stories from the project.
    </p>
  </div>
</section>

<!-- Posts -->
<section class="py-12 md:py-16">
  <div class="max-w-3xl mx-auto px-5 sm:px-8">
    {{ if posts && posts.items && posts.items.length > 0 }}
      <div class="space-y-1">
        {{ for post of posts.items }}
          <a href="/blog/{{ post.slug }}"
            class="group block p-5 -mx-5 rounded-xl hover:bg-gray-100/80 dark:hover:bg-gray-800/50 transition-colors">
            <h2 class="text-xl font-semibold text-gray-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors mb-2">
              {{ post.title }}
            </h2>
            <p class="text-gray-500 dark:text-gray-400 line-clamp-2 leading-relaxed mb-2">
              {{ post.body }}
            </p>
            <span class="text-sm font-medium text-blue-600 dark:text-blue-400 inline-flex items-center gap-1">
              Read more
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-3.5 h-3.5 transition-transform group-hover:translate-x-0.5"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
            </span>
          </a>
        {{ /for }}
      </div>
    {{ else }}
      <div class="text-center py-20">
        <div class="w-14 h-14 rounded-2xl bg-gray-100 dark:bg-gray-800 flex items-center justify-center mx-auto mb-5">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-7 h-7 text-gray-400 dark:text-gray-500"><path d="M12 20h9"/><path d="M16.376 3.622a1 1 0 0 1 3.002 3.002L7.368 18.635a2 2 0 0 1-.855.506l-2.872.838a.5.5 0 0 1-.62-.62l.838-2.872a2 2 0 0 1 .506-.854z"/></svg>
        </div>
        <p class="text-gray-500 dark:text-gray-400 text-lg">No posts available yet.</p>
      </div>
    {{ /if }}

    {{ if posts && posts.totalPages > 1 }}
      <nav class="flex items-center justify-center gap-3 mt-12 pt-8 border-t border-gray-200/60 dark:border-gray-700/40">
        {{ if posts.hasPrev }}
          <a href="{{ posts.prevUrl }}"
            class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-400 bg-white dark:bg-gray-800 border border-gray-200/60 dark:border-gray-700/40 rounded-xl hover:border-blue-300 dark:hover:border-blue-500/30 transition-colors">
            Previous
          </a>
        {{ /if }}

        <span class="text-sm text-gray-400 dark:text-gray-500">
          Page {{ posts.page }} of {{ posts.totalPages }}
        </span>

        {{ if posts.hasNext }}
          <a href="{{ posts.nextUrl }}"
            class="px-4 py-2 text-sm font-medium text-white bg-gray-900 dark:bg-white dark:text-gray-900 rounded-xl hover:bg-gray-800 dark:hover:bg-gray-100 transition-colors">
            Next
          </a>
        {{ /if }}
      </nav>
    {{ /if }}
  </div>
</section>
